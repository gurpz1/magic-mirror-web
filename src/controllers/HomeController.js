(function() {
	'use strict';

	angular.module('magicMirror')
	.controller('homeController', [
		'$scope',
		'$mdToast',
		'$window',
		'$interval',
		'$log',
		'$http',
		function($scope, $mdToast, $window, $interval, $log, $http) {

		}]);

})();